test('renders without crashing', () => {
    expect(true).toBe(true);
  });
  

  test('1 + 2는 3이어야 한다', () => {
    expect(1 + 2).toBe(3);
  });

  test('0 + 0는 0이어야 한다', () => {
    expect(0 + 0).toBe(0);
  });